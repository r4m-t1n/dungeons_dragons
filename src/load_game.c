#include <stdlib.h>
#include <stdio.h>

void load_game(void){
    // struct Game a = load_saved_games()
    
    //int load_game_info[4] = {a.life, a.coin, a.items, a.completed_m};
    // start_game(*load_game_info);
}